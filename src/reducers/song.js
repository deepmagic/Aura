const initialState = {
    title: 'song-name',
    tempo: 120,
    timeSignature: 4,
}

export const song = (state = initialState, action) => {
    switch(action.type) {
        default:
            return state
    }
}
